# Workspace Rules & Instructions

## Auto-Commit Rule
- Whenever you make changes to files, add features, fix bugs, or modify the codebase, always automatically stage and commit the changes to Git using `git add .` and a concise, descriptive conventional commit message (e.g., `feat: ...`, `fix: ...`).
- Ensure any compilation/build checks pass before committing.
